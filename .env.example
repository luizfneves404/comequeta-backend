DATABASE_URL=sqlite:///./comequeta.db
DEBUG=false

# Auth / JWT
# Generate a strong secret with: openssl rand -hex 32
JWT_SECRET_KEY=dev-insecure-change-me-with-openssl-rand-hex-32
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60
